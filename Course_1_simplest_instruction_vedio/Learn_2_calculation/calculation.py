# 运算符
# + - * /
# % 取模，** 幂，//整除

# 比较运算符
# == 相等，!= or <> 不等，>, <, >=, <=

# 赋值运算符
# = 赋值，
# a += b, a = a + b;
# a -= b, a = a - b
# a *= b, a/=b
# a %= b, a = a % b
# a **= b, a = a**b
# a //=b, a = a // b

# 逻辑运算符
# and, or, not

# 成员运算符
# in， not in，返回 True 或 False

# 身份运算符
# is， not is，是否引自同一个变量(是否相等）

# 位运算符
# 有：& 与，| 或，^ 异或，~取反，<< 左移，>>右移

a = 1
b = 2
print(a/b)